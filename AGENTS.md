# Novel Studio 项目协作约定

## QoderWork × 豆包 分工（省 token 纪律）

**总原则**：豆包负责「想和写」（开放、冗长、可反复迭代），QoderWork 负责「做和验」（动文件、跑命令、多步验证、调用项目记忆）。凡能先产出"一份明确结论/草稿"的活，都先在豆包做完，QoderWork 只接精炼输入做收尾落地。

### 交给豆包（用户侧，量大便宜）
- 长文起草：手册章节、报告、验收文档、小说正文/判词/小传等创作内容。
- 长文档阅读浓缩：外部 SRS、诊断报告、论文 → 压成一页"事实+结论"简报再交给 QoderWork。
- 头脑风暴：起名、大纲推演、方案对比、多版本候选，迭代到满意为止。
- 孤立函数初稿：规格清晰、不依赖本代码库上下文的单个函数/算法。
- 翻译、改写、格式转换、批量文本处理。

### 留给 QoderWork（需工具/文件访问/项目记忆）
- 改代码、跑 tsc/测试、git、构建部署。
- 需读真实代码库才能定位的探索与修改。
- 多步带验证的闭环（改→编译→修→再验）。
- 依赖跨会话记忆的活（负数ID约定、双库结构、历史坑位）。

### QoderWork 执行纪律（每次开工自动遵守）
1. **默认"豆包出稿、我集成"**：收到开放性/冗长任务，先确认是否已有豆包草稿或简报；没有且适合豆包做的，提示用户先去豆包出稿，而非自己从零铺量。
2. **精确小范围优先**：按用户给的范围动手，不做无谓的全库探索，减少读文件的 token 消耗。
3. **长文档先要简报**：用户直接丢长文档时，优先基于其结论行动，避免整文反复读。
4. **批量处理**：多个相关改动合并成一次往返，避免逐项来回。
5. **不重复造轮子**：复用项目既有 hook/组件/约定（见技术手册），不另起炉灶。

### 更大的省钱杠杆（备查）
XianxiaForge 应用内生成管线（writer/auditor/branch 等 agent）才是 token 大头。若豆包走火山方舟 API 单价低，可将 LLM 配置的 `LLM_BASE_URL` 指向豆包模型，让应用内生成/审计直接用豆包跑。涉及此项改动时主动评估可行性。
